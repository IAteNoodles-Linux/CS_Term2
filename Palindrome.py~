# Checks if a number is palindrome or not.

def isPalindrome(n):
    return str(n) == str(n)[::-1]

if __name__ == '__main__':
    n = int(input("Enter a number: "))
    print("{} is {}palindrome".format(n,"not " if not isPalindrome(n) else ""))

